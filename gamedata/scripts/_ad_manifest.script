--- _ad_manifest: AlifeDiegetic identity. Pure data, no logic. Read by _ad_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifeDiegetic",
    version = "next",
    xlibs   = "1.8.4",
}
